# Generated by analyze.py — fill in root/synth_root values
# Song: Mus8F - Game Over.asm

name: Game Over
input_file: sonic_1\music\Mus8F - Game Over.asm
output_file: output/19_game_over.mod
samples_dir: "samples/"

auto_bpm: true
target_speed: 3
ticks_per_row: 1
num_mod_channels: 8
region: ntsc

channels:
  - source: DAC
    mod_channel: 0
  - source: FM1
    mod_channel: 1
  - source: FM2
    mod_channel: 2
  - source: FM3
    mod_channel: 3
  - source: FM4
    mod_channel: 4
  - source: FM5
    mod_channel: 5

sample_list:
  # --- percussion ---
  - [1, "kick.raw", 64, 0]
  # --- voice $00: alg 2: (1+(2→3))→4, fb 7 — FM1 ---
  - [2, "fm_v00.raw", 32, 0]
  # --- voice $01: alg 4: (1→2)+(3→4), fb 7 — FM2, FM3 ---
  - [3, "fm_v01.raw", 32, 0]
  # --- voice $02: alg 2: (1+(2→3))→4, fb 7 — FM4 ---
  - [4, "fm_v02.raw", 32, 0]
  # --- voice $03: alg 7: 1+2+3+4, fb 3 — FM5 ---
  - [5, "fm_v03.raw", 32, 0]

dac_samples:
  # dKick: ×4 — native ~8250 Hz → note C2
  - name: dKick
    mod_instrument: 1
    mod_note: C2

voice_map:
  # $00 — alg 2: (1+(2→3))→4, fb 7 — used by FM1
  0:
    - low:  G5
      high: D6
      mod_instrument: 2
      root: G2
      synth_root: G4
  # $01 — alg 4: (1→2)+(3→4), fb 7 — used by FM2, FM3
  1:
    - low:  A6
      high: G7
      mod_instrument: 3
      root: A2
      synth_root: A6
  # $02 — alg 2: (1+(2→3))→4, fb 7 — used by FM4
  2:
    - low:  E3
      high: D4
      mod_instrument: 4
      root: E2
      synth_root: E3
  # $03 — alg 7: 1+2+3+4, fb 3 — used by FM5
  3:
    - low:  D7
      high: G7
      mod_instrument: 5
      root: D2
      synth_root: D5
